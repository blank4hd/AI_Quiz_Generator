# Google Gemini API Key (Required)
# Get your API key from: https://makersuite.google.com/app/apikey
# This key can also be used for YouTube Data API if enabled in Google Cloud Console
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key_here

# YouTube Transcript Configuration (Optional)
# Method 1: Python API - No setup required, works immediately
# Method 2: Google YouTube Data API - Requires enabling in Google Cloud Console
# If using Method 2, you can use the same GOOGLE_GEMINI_API_KEY above
# Or provide a separate API key in the UI
# Setup guide: See YOUTUBE_SETUP.md
